(() => { let q = s => document.querySelector(s), u = () => { let e = q("[data-internet-time-value]"); e && (e.textContent = "@" + (((Date.now() + 36e5) % 864e5) / 86400).toFixed(2).padStart(6, "0")) }, m = () => { let s = q(".banner-status"); if (!s || q("[data-internet-time-value]")) return; let r = s.lastElementChild || s.appendChild(document.createElement("div")); r.innerHTML = '<span class="text-lime">SYSTEM TIME:</span> <span class="internet-time-value" data-internet-time-value>@000.00</span>'; u() }; document.readyState === "loading" ? document.addEventListener("DOMContentLoaded", m) : m(); u(); setInterval(u, 1e3) })();